import axios from 'axios';


export const getAirport = (airport_id) => {
    return axios.get(`/api/airports/${airport_id}`)
}

export const getAirport = (airport_id) => {
    return axios.get(`/api/airports/${airport_id}`)
}